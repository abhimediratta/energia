import { rgba } from 'polished';

const colors = {
  header: {
    background: '#262626',
    color: '#fff'
  },
  content: {
    background: '#eee',
    color: '#000'
  },
  categoryItem: {
    background: rgba('#000', 0.7),
    color: '#fff'
  }
};

const theme = {
  colors
};

export default theme;
